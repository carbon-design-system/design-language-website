name: Optimize SVGs
on:
  pull_request:
    paths:
    - "**.svg"

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: ericcornelissen/svgo-action@next
      with:
        repo-token: "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"
        svgo-options: ".github/svgo.config.js"
